# Server-side origin used only by Next.js rewrites. Keep backend session traffic same-origin in the browser.
AMANA_BACKEND_ORIGIN=http://localhost:8080

# Non-secret demo merchant tenant IDs from the deployed bootstrap, used to label account-link status.
NEXT_PUBLIC_AMAZING_MERCHANT_ID=
NEXT_PUBLIC_FRESHBASKET_MERCHANT_ID=
