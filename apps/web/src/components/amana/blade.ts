/**
 * Explicit opt-in entry for application routes that need Blade's interactive
 * runtime. Public/server-only surfaces must import from `@/components/amana`.
 */
export { AmanaButton } from "./amana-button";
export { AmanaProvider } from "./amana-provider";
